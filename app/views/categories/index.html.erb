<div class="w-100 d-flex">
  <h1 class="flex-grow-1">Categorias</h1>
  <div>
    <%= link_to "Nova Categoria", new_category_path, class: "btn btn-primary" %>
  </div>
</div>

<div class="d-flex flex-column gap-4">
  <%#= render partial: "category", collection: @categories, as: :category %>
  <%= render @categories %>
</div>